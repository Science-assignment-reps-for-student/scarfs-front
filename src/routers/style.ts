import styled from 'styled-components';
import { BannerImage } from '../assets/Board/Default';

export const Banner = styled.div`
  background-image: url(${BannerImage});
  min-width: 1340px;
  height: 150px;
  background-size: cover;
`;
